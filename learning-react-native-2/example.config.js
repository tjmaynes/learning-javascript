const config = {
  apiKey: "some-long-hash",
  authDomain: "some-domain-url",
  databaseURL: "some-database-url",
  projectId: "some-project-id",
  storageBucket: "some-storage-bucket",
  messagingSenderId: "some-sender-id"
};

export default config;
